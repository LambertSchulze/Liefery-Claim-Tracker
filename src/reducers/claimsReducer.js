export const initialState = {
    claims:     [],
    loading:    false,
    hasErrors:  false,
}

export default function claimsReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}